import React from 'react';
import Card from '../components/Card';

const AddNewUser: React.FC = () => (
  <div className="py-6">
    <Card title="Add New User">
      <p>This is the Add New User page.</p>
    </Card>
  </div>
);

export default AddNewUser; 